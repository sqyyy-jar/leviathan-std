(mod assembly)

(+label strlen (do ; (*str)
  (mov r1 0u) ; len = 0
  (do-while (do
    (add r1 r1 1u) ; len += 1
    (ldrb r2 r0 0) ; b = *str
    (add r0 r0 1u) ; str += 1
  ) !0 r2) ; while b != 0
  (sub r0 r1 1u) ; return len - 1
))
